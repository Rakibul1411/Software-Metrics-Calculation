# Output Directory

This directory contains the generated CSV files with calculated metrics.

Generated files will have names like:
- `ant-1.3-calculated.csv`
- `metrics.csv` (default)

The CSV files are in `.gitignore` to avoid committing large datasets.
